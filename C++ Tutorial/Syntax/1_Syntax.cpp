#include <iostream> // Header File Library with input and output objects like cout
using namespace std; // we can use names for objects and variables ffrom the standard library

int main() {
    cout << "Hello World!"; // cout (c output) is an object used together with the insertion operator << to print text
    return 0;
}

// some pprograms omit namespace and replaced with  the std keyword
// std::cout << "Hello World!"